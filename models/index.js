//Import the models into this file, it will package up all the models
const User = require("./User");
const Thoughts = require("./Thoughts");

module.exports = { User, Thoughts };
//this will be imported in ./controllers/
